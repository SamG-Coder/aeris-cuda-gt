import {cp,rm,mkdir} from 'node:fs/promises';const root=new URL('../',import.meta.url),dist=new URL('../dist/',import.meta.url);await rm(dist,{recursive:true,force:true});await mkdir(dist,{recursive:true});for(const file of ['index.html','src','kernels','vendor','assets','LICENSE','THIRD_PARTY_NOTICES.md'])await cp(new URL(file,root),new URL(file,dist),{recursive:true});console.log('Built dist/: self-contained HTTPS static site.');
