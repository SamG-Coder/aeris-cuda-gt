import {readFile,mkdir,writeFile} from 'node:fs/promises';import {compile} from '../vendor/cuda-webshader/src/compiler/compiler.js';
export const entries={car:['describeCar','sampleCar','buildCar','bakeSurface'],vehicle:['resetVehicle','stepVehicle','tireForces','integrateVehicle','buildPose','updateEffects']};await mkdir(new URL('../generated/',import.meta.url),{recursive:true});let fails=0;
for(const [file,names]of Object.entries(entries)){let source;try{source=await readFile(new URL('../kernels/'+file+'.cu',import.meta.url),'utf8');}catch{continue;}for(const entry of names)try{const artifact=compile(source,{entry,workgroupSize:[128,1,1]});await writeFile(new URL('../generated/'+entry+'.wgsl',import.meta.url),artifact.wgsl);console.log('PASS',entry,artifact.wgsl.length);}catch(e){fails++;console.error('FAIL',entry,e.message);}}process.exitCode=fails?1:0;
